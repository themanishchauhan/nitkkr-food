---
interface Props {
  chips?: Array<{ label: string; icon: string; href: string; isEnabled?: boolean }>;
}

const defaultChips = [
  { label: 'High Protein 🏋️‍♂️', icon: '⚡', href: '/search?tag=protein', isEnabled: true },
  { label: 'Late Night 2AM 🌙', icon: '🌙', href: '/search?tag=late-night', isEnabled: true },
  { label: 'Under ₹50 💸', icon: '💰', href: '/search?maxPrice=50', isEnabled: true },
  { label: 'Spicy Momos 🔥', icon: '🥢', href: '/search?q=momos', isEnabled: true },
  { label: 'Cheesy & Loaded 🧀', icon: '🧀', href: '/search?q=cheese', isEnabled: true },
  { label: 'Thick Shakes 🧋', icon: '🧋', href: '/search?category=beverages', isEnabled: true },
  { label: 'Pure Veg 🌱', icon: '🌱', href: '/search?isVeg=true', isEnabled: true },
  { label: 'Non-Veg 🍗', icon: '🍗', href: '/search?isVeg=false', isEnabled: true },
];

const { chips = defaultChips } = Astro.props;
const activeChips = (chips && chips.length > 0 ? chips : defaultChips).filter(c => c.isEnabled !== false);
---

<div class="flex overflow-x-auto gap-2 pb-2 mb-6 -mx-4 px-4 sm:mx-0 sm:px-0 sm:flex-wrap sm:justify-center scrollbar-none" role="group" aria-label="Quick student filters">
  {activeChips.map(chip => (
    <a
      href={chip.href}
      class="px-3.5 py-2 bg-white border border-amber-200/80 text-slate-800 rounded-full text-xs font-bold
             hover:bg-gradient-to-r hover:from-amber-500 hover:to-orange-500 hover:text-white hover:border-transparent shadow-2xs transition-all duration-200 flex items-center gap-1.5 whitespace-nowrap active:scale-95"
    >
      <span aria-hidden="true">{chip.icon}</span>
      <span>{chip.label}</span>
    </a>
  ))}
</div>